# The bundled reference adapters (mock_backend.py, comfyui_adapter.py,
# fal_adapter.py) are now STDLIB-ONLY — they need nothing here, so Pallaidium's
# "Start Backend" button can launch them with Blender's own Python (no install).
#
# This file is kept only as a hint for people writing fancier custom adapters
# who prefer a web framework / HTTP client. None of it is required by the
# examples above. Uncomment what you actually use:
#
# fastapi>=0.110
# uvicorn>=0.29
# httpx>=0.27
# fal-client>=0.4     # only if you rewrite fal_adapter.py to use the SDK
